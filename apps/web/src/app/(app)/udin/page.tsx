import { BadgeCheck } from "lucide-react";
import { ComingSoonPage } from "@/components/layout/coming-soon-page";

export default function UdinPage() {
  return (
    <ComingSoonPage
      title="UDIN"
      icon={BadgeCheck}
      description="UDIN tracking for certificates, audits and reports is planned as an integration-ready module — not built yet."
    />
  );
}
