import { ClipboardCheck } from "lucide-react";
import { ComingSoonPage } from "@/components/layout/coming-soon-page";

export default function AuditPage() {
  return (
    <ComingSoonPage
      title="Audit"
      icon={ClipboardCheck}
      description="Audit engagements, planning, working papers, review and UDIN linkage are planned — not built yet."
    />
  );
}
